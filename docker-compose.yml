services:
  backend:
    build:
      context: ./backend

  frontend:
    build:
      context: ./frontend
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
